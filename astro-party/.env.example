# Astro Party environment template
#
# Local development defaults:
# - HTTP endpoint for match-making and room APIs
# - WebSocket endpoint for Colyseus realtime connection
#
# For production builds, replace these with your deployed server URLs
# before running: bun run build

VITE_MATCH_HTTP_URL=http://localhost:2567
VITE_COLYSEUS_WS_URL=ws://localhost:2567